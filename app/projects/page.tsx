import { ModuleShell } from "../components/ModuleShell";

export default function ProjectsPage() {
  return (
    <ModuleShell
      name="Project Hub"
      icon="⬡"
      description="Every project in one place — status, next task, GitHub link, deadline. Nothing falls through."
      status="active"
      note="Projects: RutaSmart (thesis) · CareerOS (Phase 2) · LJR.devOS (this) — UI coming soon"
    />
  );
}
