import { ModuleShell } from "../components/ModuleShell";

export default function CareerPage() {
  return (
    <ModuleShell
      name="CareerOS"
      icon="◈"
      description="Job application engine. Scores roles with KYN, tracks applications, feeds data back into the OS."
      status="building"
      note="Phase 2 — /analyze command in progress (lives in the Tempo repo)"
    />
  );
}
